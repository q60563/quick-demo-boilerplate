import React from 'react';

var TemperatureCard = React.createClass({
    render: function () {
        return (
            <div style={{width: '100%', height: '100%', backgroundColor: '#30FFFF'}}>
                TemperatureCard
            </div>
        );
    }
});


module.exports = TemperatureCard;
